import React from 'react'

const Home = () => {
	return (
		<div>
			Logs for all the logs characters for all the characters. putting diary shit here later gotta type fast or else
			it'll be too long.(or maybe not)(those two things were put in by git copilot. wow)
		</div>
	)
}

export default Home
